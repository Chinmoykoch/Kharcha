import 'package:flutter/material.dart';

class PersonalAssistantScreen extends StatelessWidget {
  const PersonalAssistantScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
